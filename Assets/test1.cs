using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*if (gameObject.GetComponent<_2dxFX_DesintegrationFX>().Desintegration < 0.9f)
        {
            gameObject.GetComponent<_2dxFX_DesintegrationFX>().Desintegration += 0.01f;
        }
        else
        {
            gameObject.GetComponent<_2dxFX_DesintegrationFX>().Desintegration = 1f;
            Destroy(this);
        }*/
   
        
    }
}
